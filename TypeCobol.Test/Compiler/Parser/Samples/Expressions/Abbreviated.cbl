IF A.
IF (A).
IF (A = 1).
IF A = B.
IF A = 'B'.
IF (A = B OR C).
IF (A = (B OR < C)).
IF (NOT A = (B AND < C)).
IF NOT (A = (B AND < C)).
IF NOT (x = ('a' OR 'b')) AND NOT ( (y = 'c') AND ( z = ('d' OR 'f' AND NOT < 'g' AND 'h' OR NOT > 'i' OR 'j' AND 'k' OR NOT 'l') ) ).
IF x NOT < y IF z CONTINUE.
IF        x 
  AND NOT y 
  AND NOT z 
  CONTINUE.