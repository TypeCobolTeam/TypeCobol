       id division.
       program-id. PgmA.
       namespace banking::currentaccount.
       data division.
       WORKING-STORAGE SECTION.
       01 MyType typedef strict public pic X.  
       procedure division.
       .
Line 9[8,37] <27, Error, Syntax> - Syntax error : Nested program "nested" cannot declare namespace
       id division.
       program-id. nested.
       namespace banking::currentaccount::bill.
      *Using clause = allow to access all type in
       using banking::currentaccount.
       data division.
       WORKING-STORAGE SECTION.
      *> Ko, because we need at least the program name PgmA to find the type
Line 17[8,27] <30, Error, Semantics> - Semantic error: TYPE 'MyType' is not referenced
       01 var1 type MyType.
      *> Ok, because we can find PgmA with the using clause
       01 var2 type PgmA::MyType. 
      *> full namespace Ok
Line 21[8,8] <30, Error, Semantics> - Semantic error: TYPE 'banking.currentaccount.PgmA.PgmA.MyType' is not referenced
       01 var3 type banking::currentaccount::PgmA::MyType 
       .
       end program nested.

       end program PgmA.
Line 26[8,47] <27, Error, Syntax> - Syntax error : Stacked program "PgmB" cannot declare namespace
       id division.
       program-id. PgmB.
       namespace banking::currentaccount::bill.
       data division.
       WORKING-STORAGE SECTION.
      *> invalid because there is no using and no program prefix
Line 32[8,28] <30, Error, Semantics> - Semantic error: TYPE 'MyType' is not referenced
       01 var1 type MyType. 
      *> KO, because PgmA is not referenced by an using clause
       01 var2 type PgmA::MyType.
      *> full namespace Ok
Line 36[8,8] <30, Error, Semantics> - Semantic error: TYPE 'banking.currentaccount.PgmA.PgmA.MyType' is not referenced
       01 var3 type banking::currentaccount::PgmA::MyType 
       .
       end program PgmB.
       
       id division.
       program-id. PgmC.
      * namespace banking::currentaccount::bill.
      *Using clause = allow to access all type in
       using banking::currentaccount.
       data division.
       WORKING-STORAGE SECTION.
      *> Ko, because we need at least the program name PgmA to find the type
Line 48[8,27] <30, Error, Semantics> - Semantic error: TYPE 'MyType' is not referenced
       01 var1 type MyType.
      *> Ok, because we can find PgmA with the using clause
       01 var2 type PgmA::MyType. 
      *> full namespace Ok
Line 52[8,8] <30, Error, Semantics> - Semantic error: TYPE 'banking.currentaccount.PgmA.PgmA.MyType' is not referenced
       01 var3 type banking::currentaccount::PgmA::MyType 
       .
       end program PgmC.
